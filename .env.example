# Copy this file to `.env` and fill in real values. Do NOT commit your `.env` file to git.

# Example DATABASE_URL for Postgres:
# DATABASE_URL=postgresql://username:password@localhost:5432/dbname

# App settings
# PORT=5000
# NODE_ENV=development

# OAuth / third-party keys (placeholders)
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret

# Add any other env vars used in your app here (see README or server code)
